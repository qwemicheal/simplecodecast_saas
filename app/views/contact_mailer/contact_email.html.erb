<!doctype html>
<html>

    <head>
    
    </head>
    <body>
        <p>You have received a message from the sites's contyact form, from <%= "#{@name},#{@email}."%></p>
        <p><%%= @body %></p>
    </body>
</html>